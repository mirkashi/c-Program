#include <iostream>

int main() {
    // Syntax: for (initialization; condition; increment/decrement)
    for (int i = 0; i < 5; ++i) {
        std::cout << "Iteration " << i + 1 << std::endl;
    }
    return 0;
}
